{{#adverts}}
    <div class="adverts">
        <ul>
            {{#adverts}}
                <li>
                    <span class="advice">Advertisment</span>
                        <span class="advert">
                            <img src="{{assets}}/{{image}}" alt="{{title}}" title="{{title}}">
                        </span>
                </li>
            {{/adverts}}
        </ul>
    </div>
{{/adverts}}
